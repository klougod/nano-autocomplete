export const listId = (input_id) => { return `${input_id}-autocomplete-list`; }
export const listClassName = 'autocomplete-items';
export const itemClassName = 'autocomplete-item';
export const activeItemClassName = 'autocomplete-active';